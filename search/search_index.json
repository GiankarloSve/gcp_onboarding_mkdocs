{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to MkDocs","text":"<p>For full documentation visit mkdocs.org.</p>"},{"location":"#commands","title":"Commands","text":"<ul> <li><code>mkdocs new [dir-name]</code> - Create a new project.</li> <li><code>mkdocs serve</code> - Start the live-reloading docs server.</li> <li><code>mkdocs build</code> - Build the documentation site.</li> <li><code>mkdocs -h</code> - Print help message and exit.</li> </ul>"},{"location":"#project-layout","title":"Project layout","text":"<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre>"},{"location":"onboarding_new_gcp_users/","title":"Risk Analysis Modeler","text":""},{"location":"onboarding_new_gcp_users/#onboarding-new-gcp-users-model-developers","title":"Onboarding New GCP Users \u2014 Model Developers","text":""},{"location":"onboarding_new_gcp_users/#introduction","title":"Introduction","text":"<p>GCP is our bank's Global Risk Management toolkit, designed to speed up Python development. This includes the simplification of querying any database that business supports across the banking division and investment division. Throughout this document, development and developers refer to the team that has designed and maintains this product, and makes it available to other \"customer\" departments. Model developers are the Python programmers or developers that will be using this documentation to get started and to create customized risk analysis solutions for their teams.</p> <p>This document explains how a workflow is created, defines the most important classes and nodes, and provides exercises that will help you set up your environment. Each exercise builds on the previous one \u2014 and each iteration expands the complexity of your challenges.</p> <p></p>"},{"location":"onboarding_new_gcp_users/#setting-up","title":"Setting Up","text":"<p>The following instructions concisely summarize how to set up your environment.</p> <p>If you are new to our bank, you may find these instructions too brief. Consult your department's Confluence and Wiki documentation for business process constraints or procedures that are unique to your department. This section covers:</p> <ol> <li>Getting the right software</li> <li>Installing Python and Git</li> <li>Getting the right permissions</li> <li>Setting up a development environment on your PC</li> </ol>"},{"location":"onboarding_new_gcp_users/#getting-the-right-software","title":"Getting the Right Software","text":"<p>Installing Python, PyCharm, and Git (includes Git Bash) is a straightforward process. Visit the myTechnology website and request each of these tools. After about half a day, you will receive an automated email notifying you that the PAM system has delivered these applications to your desktop. Launch them to confirm they have been installed properly. If you have any issues running them, submit an L1 ticket via the ServiceNow portal link provided in your department's \"Welcome Packet: Onboarding Guide.\"</p> <p>To install PyCharm Community Edition, visit myTechnology.</p> <p>When requesting PyCharm or any other tools mentioned in this document, select the most recent, free version. Refer to the next section, which primarily focuses on adding a security key into BitBucket.</p> <p>Important: You will not be able to pull repositories from the GCP Bitbucket project without proper security key setup.</p>"},{"location":"onboarding_new_gcp_users/#installing-python-and-git","title":"Installing Python and Git","text":"<p>To install Python (current recommended version is 3.9), go to Python x64 3.9.20 and click Request Now. If your department blocks this operation, submit a ticket via myTechnology python-3920. For EA Request ID, select Not Required from the pull-down menu. When done, click Submit request. You will be notified via email when it has been installed to your endpoint (laptop, device, or virtual machine).</p> <p>To install Git, there are a few additional steps because Git must pass BitBucket security, requiring the <code>.gitconfig</code> file to be set up. Specifically, you need to:</p> <ol> <li>Install Git using the myTechnology website</li> <li>Set up basic Git Bash properties</li> <li>Set up BitBucket SSO</li> </ol> <p>Each will be explained below.</p>"},{"location":"onboarding_new_gcp_users/#1-install-git-using-the-mytechnology-website","title":"1. Install Git Using the myTechnology Website","text":"<ol> <li>Go to the myTechnology website and search for Git.</li> <li>Click the Request Now button next to the latest version (Example: Git 2.46.1).</li> <li>Fill out the required request information and click Submit request.</li> <li>Wait for an email informing you that Git has been successfully installed on your device.</li> </ol>"},{"location":"onboarding_new_gcp_users/#2-set-up-basic-git-bash-properties","title":"2. Set up Basic Git Bash Properties","text":"<p>After you are notified that Git has been installed, follow these steps:</p> <ol> <li>Click Start and search for Git.</li> <li>When the Git Bash icon appears, right-click and select Pin to Start.</li> <li>Launch Git Bash.</li> <li>To open your <code>.gitconfig</code> file for editing, run the following command:    <code>bash    git config --global --edit</code></li> <li> <p>In vscode or a text editor, e.g., vi, or Notepad++, update your e-mail and name. Make sure that this email address is the one being used by BitBucket, because this becomes important for the next section. Example:</p> <p><code>python [user]     name = Doe, Jon     email = jon.doe@bofoz.com</code></p> <p>Tip: If a <code>.gitconfig</code> file does not exist, Git Bash will automatically create it when you run the edit command.</p> </li> </ol>"},{"location":"onboarding_new_gcp_users/#set-up-bitbucket-sso","title":"Set Up Bitbucket SSO","text":"<p>In this step, you will set up an ed25519 key and integrate it with your Bitbucket account. ED25519 is equivalent to RSA-3072 in that it provides 128-bit security, and it is commonly used in PGP and TLS. It is practical even for IoT solutions because it is fast and efficient on low-powered devices.</p> <p>To set up Bitbucket:</p> <ol> <li> <p>Check for an existing SSH key by entering the following command in your Git Bash root.</p> <p>The directory found when the Git Bash session is invoked: ```bash</p>"},{"location":"onboarding_new_gcp_users/#the-public-ssh-key-used-by-bitbucket-is-found-in-id_ed25519pub","title":"The public SSH key used by Bitbucket is found in id_ed25519.pub","text":""},{"location":"onboarding_new_gcp_users/#if-you-have-this-file-you-have-already-completed-this-section-on-preparing-python-and-git","title":"If you have this file, you have already completed this section on preparing Python and Git","text":"<p>ls -al ~/.ssh</p>"},{"location":"onboarding_new_gcp_users/#if-you-receive-the-error-ls-cannot-access-cssh-no-such-file-or-directory","title":"If you receive the error: \u2018ls: cannot access /c/.ssh: No such file or directory\u2019","text":""},{"location":"onboarding_new_gcp_users/#you-can-resolve-it-by-creating-an-ssh-directory-in-your-home-directory","title":"You can resolve it by creating an .ssh directory in your home directory.","text":""},{"location":"onboarding_new_gcp_users/#it-should-path-to-cusersssh","title":"It should path to C:/Users//.ssh <p>mkdir ${HOME}/.ssh ```</p> <p>Screenshot: Output result</p> <p></p>  <p>Tip: If you already have <code>id_ed25519</code> and <code>id_ed25519.pub</code> but wish to generate a new key, delete these files and continue to the next step to create a new SSH key. If you believe Bitbucket is already set up and wish to test it, jump to Step 7 of this section.</p>  <p>If you cannot run the <code>ls</code> command, make sure it exists and is pathed correctly. Troubleshooting Examples: ```bash</p>","text":""},{"location":"onboarding_new_gcp_users/#returns-the-lsexe-path-typically-found-in-usrbin-or-program-filesgitusrbin","title":"Returns the ls.exe path (typically found in /usr/bin/ or /Program Files/git/usr/bin/): <p>which ls</p>","text":""},{"location":"onboarding_new_gcp_users/#provides-multiple-locations-including-binary-source-and-manual-pages","title":"Provides multiple locations, including binary, source, and manual pages: <p>whereis ls</p>","text":""},{"location":"onboarding_new_gcp_users/#to-test-if-it-is-in-a-valid-path","title":"To test if it is in a valid path: <p>echo $PATH ```</p>  <li> <p>Generate a new SSH key by creating <code>id_ed25519</code> and <code>id_ed25519.pub</code> with <code>git config</code>. In Git Bash, run the following commands, replacing the placeholder values with your actual name and email. Do not enter a passphrase.</p> <p>Example: ```python git config --global user.name \"Last, First\" git config --global user.email your_email@BofOz.com</p>","text":""},{"location":"onboarding_new_gcp_users/#generate-a-new-ssh-key","title":"Generate a new SSH key <p>ssh-keygen -t ed25519 -C your_email@BofOz.com</p>","text":""},{"location":"onboarding_new_gcp_users/#when-prompted-to-enter-a-file-name-for-saving-your-new-key-press-to-accept-the-default-location","title":"When prompted to enter a file name for saving your new key, press  to accept the default location.","text":""},{"location":"onboarding_new_gcp_users/#do-not-enter-a-passphrase","title":"Do NOT enter a passphrase. <p>```</p>  <li> <p>Retrieve and verify your SSH key. To get the content of the <code>id_ed25519.pub</code> file, use the <code>cat</code> and <code>clip</code> commands in Git Bash (you will need this soon for Bitbucket).</p> <p>Examples: ```bat</p> </li> <li> <p>Log into your Bitbucket account.     After entering Bitbucket, click on your Profile (top right) and select Manage account.</p> <p>Screenshot: Finding Bitbucket's Manage Account settings </p> </li> <li> <p>Paste the contents of your <code>id_ed25519.pub</code> file into Bitbucket:</p> <ul> <li>Select SSH keys from the left menu.</li> <li>Click Add key.</li> <li>Paste your unique SSH key into the Key text field.</li> <li>Optionally, enter a label in the Key label field for traceability.</li> <li>Click Save to complete your Bitbucket setup.</li> </ul> </li> <li> <p>After saving, you can view your SSH key:</p> <ul> <li>Go to Profile &gt; Manage account &gt; SSH keys.</li> </ul> <p>Screenshot: Viewing all Bitbucket keys </p> </li> <li> <p>Test your SSH connection to Bitbucket.</p> <ul> <li>Open Git Bash or your terminal.</li> <li>Run the following command to verify that your SSH key is correctly configured: <code>python ssh -T git@scm.horizon.bankofoz.com</code></li> <li>If the connection is successful, you will see a welcome message indicating successful authentication.</li> <li>If you receive a permission error, review your SSH key setup before continuing.</li> </ul>  <p>Tip: Testing your SSH connection early saves time troubleshooting later.</p>  </li> <li> <p>Confirm that your key works by cloning a test repository.</p> <ul> <li>Click on Projects in the main Bitbucket ribbon.</li> <li>In Search (Filter projects by name), enter GRA.</li> <li>In the filter results, click on GRA Core Platform.</li> <li>In Search (Filter repositories by name), enter play.</li> <li>In the filter results, click on playground.</li> <li>In Bitbucket\u2019s left-panel ribbon, click Clone and copy the code.</li> </ul> <p>Screenshot: The Bitbucket Clone Button </p> <ul> <li>In the dialog box that appears, click the Click to copy button.</li> <li>In Git Bash, PowerShell, or Command Prompt (cmd), navigate to the directory where you want to clone the repository, then run the <code>git clone</code> command using the copied text.</li> </ul> <p>Example: ```python git clone ssh://git@scm.horizon.bankofoz.com/gcp/playground.git</p> </li> <li> <p>If you were not able to complete the above steps successfully, see Getting the Right Permissions to resolve any authorization issues.</p> </li>","text":""},{"location":"onboarding_new_gcp_users/#displays-the-ssh-public-key-you-will-copy","title":"Displays the SSH public key you will copy: <p>cat ~/.ssh/id_ed25519.pub</p>","text":""},{"location":"onboarding_new_gcp_users/#copies-your-ssh-key-directly-to-the-clipboard-for-bitbucket-setup","title":"Copies your SSH key directly to the clipboard for Bitbucket setup: <p>clip &lt; ~/.ssh/id_ed25519.pub</p>","text":""},{"location":"onboarding_new_gcp_users/#sample-output-that-is-getting-copied-and-will-soon-be-pasted-into-bitbucket","title":"SAMPLE OUTPUT that is getting copied \u2014 and will soon be pasted into Bitbucket:","text":""},{"location":"onboarding_new_gcp_users/#ssh-ed25519-aaaad5tazc1lzdi1ntresant5aaihyb5nopbdr4vrrivfjfj9ljubavrij3kaof-jondoebofozcom","title":"ssh-ed25519 AAAAD5TazC1lZDI1NTRESANT5AAIHYB5noPbdR4VrRivFjfJ9LjubavRIJ3KAof jon.doe@BofOz.com","text":""},{"location":"onboarding_new_gcp_users/#ms-dos-cmd-alternative-windows-command-prompt","title":"MS-DOS CMD alternative (Windows Command Prompt): <p>cd %userprofile%/.ssh clip &lt; id_ed25519.pub</p>","text":""},{"location":"onboarding_new_gcp_users/#macos-terminal-zsh-alternative","title":"MacOS Terminal (zsh) alternative:","text":""},{"location":"onboarding_new_gcp_users/#pbcopy-sshid_ed25519pub","title":"pbcopy &lt; ~/.ssh/id_ed25519.pub <p>```</p>","text":""},{"location":"onboarding_new_gcp_users/#if-the-following-results-are-returned-your-bitbucket-setup-is-correct","title":"If the following results are returned, your Bitbucket setup is correct: <p>Cloning into 'playground'... remote: Enumerating objects: 4, done. remote: Counting objects: 100% (4/4), done. remote: Compressing objects: 100% (4/4), done. remote: Total 4 (delta 0), reused 1 (delta 0), pack-reused 0 Receiving objects: 100% (4/4), done.</p>","text":""},{"location":"onboarding_new_gcp_users/#when-done-you-can-delete-the-playground-folder-you-just-cloned","title":"When done, you can DELETE the playground folder you just cloned.","text":""},{"location":"onboarding_new_gcp_users/#you-will-not-be-using-it-right-now","title":"You will not be using it right now. <p>```</p>","text":""},{"location":"onboarding_new_gcp_users/#getting-the-right-permissions","title":"Getting the Right Permissions","text":"<p>To access GCP repositories, GCP packages, and relevant databases, you will need permissions granted through ARM. This section briefly describes the processes for requesting permissions as a Model Developer, specifically related to GCP tools.</p>"},{"location":"onboarding_new_gcp_users/#setting-up-a-development-environment-on-your-pc","title":"Setting Up a Development Environment on Your PC","text":"<p>This section focuses on setting up Artifactory access and creating your virtual environment (venv).</p> <p>Referencing an external packaging repository (such as pip\u2019s default 'simple' index at https://pypi.org/simple/) is not permitted by most banking and financial institutions. Therefore, before you can establish a local Python learning environment or sandbox, your pip configuration must be registered with Artifactory to access an internal index.</p> <p>Once you complete this setup, you can create your Python virtual environment (venv). You will then be able to activate your tools in this virtual environment and interact with GCP, as you write your own workflows.</p> <p>Steps covered in this section:</p> <ol> <li>Log in to Oz's Artifactory</li> <li>Choose Location and Prepare pip.ini File</li> <li>Generate Python Packager Key, Update pip.ini, and Activate venv</li> <li>Troubleshooting Tips for this Section</li> </ol>"},{"location":"onboarding_new_gcp_users/#1-log-in-to-ozs-artifactory","title":"1. Log in to Oz's Artifactory","text":"<p>Bank of Oz uses a JFrog Artifactory instance to internally store and maintain resources needed by developers. To complete this section, log in to the JFrog intranet site using your Standard ID credentials.</p> <p>Tip: If this is your first time using Artifactory as a Bank of Oz employee, you may be prompted to enter your password into the Current Password field and click Unlock.</p> <p>Keep this tab open in your browser, because you will return to it in Step 3.</p>"},{"location":"onboarding_new_gcp_users/#2-choose-location-and-prepare-pipini-file","title":"2. Choose Location and Prepare pip.ini File","text":"<p>Determine where you would like to place the <code>pip.ini</code> file you will be building.</p> <p>Example:</p> <pre><code># From Bash, the pip command displays all the locations it checks for an initialization file\npip config -v list\n\n# -------------------------- Sample Output -------------------------\n# For variant 'global', will try loading 'C:\\ProgramData\\pip\\pip.ini'\n# For variant 'user', will try loading 'C:\\Users\\zkmtbsx\\pip\\pip.ini'\n# For variant 'user', will try loading 'C:\\Users\\zkmtbsx\\AppData\\Roaming\\pip\\pip.ini'\n# For variant 'site', will try loading 'C:\\dev\\gcp_py_samples\\venv\\pip.ini'\n</code></pre> <p>In one of these folders, create a <code>pip.ini</code> file, e.g., <code>C:\\ProgramData\\pip\\pip.ini</code>, and paste the following content into your new <code>.ini</code> text file:</p> <pre><code>[global]\n\ncert = /etc/pki/ca-trust/extracted/pem/tls-ca-bundle.pem\nindex-url = https://$USER:$API_KEY@artifacts.horizon.bankofoz.com/artifactory/api/pypi/virtual-pypi/simple\ntrusted-host = artifacts.horizon.bankofoz.com   pypi-python.org\n\nextra-index-url = http://$USER:$API_KEY@artifacts.horizon.bankofoz.com/artifactory/api/pypi/libs-snapshot-local-pypi/simple\n\n[search]\nindex = https://$USER:$API_KEY@artifacts.horizon.bankofoz.com  pypi.python.org\n\n[list]\nformat = columns\n\n[distutils]\nindex-servers = local\n\n[local]\nrepository = https://$USER:$API_KEY@artifacts.horizon.bankofoz.com/artifactory/api/pypi/libs-snapshots-local-pypi\n</code></pre> <p>Finally, replace each <code>$USER</code> with your <code>&lt;Standard ID&gt;</code> (e.g., zyxwvut) and save the <code>pip.ini</code> file.</p> <p>Important: You must replace \"$USER\" in four places, and your <code>&lt;Standard ID&gt;</code> must be entered in lowercase form.</p>"},{"location":"onboarding_new_gcp_users/#3-generate-python-packager-key-update-pipini-and-activate-venv","title":"3. Generate Python Packager Key, Update <code>pip.ini</code>, and Activate <code>venv</code>","text":"<p>When working with GCP for the first time, it is best to complete these twelve (12) steps in one sitting \u2014 from start to finish.</p> <p>Tip: Once your GCP environment is prepared, you may need to refer back to these steps from time to time. Whenever you start a new GCP project, revisit Step 11 and review the project package\u2019s README file.</p> <p>With your <code>pip.ini</code> still open:</p> <ol> <li>Return to your Artifactory tab.</li> <li>Log in with your Standard ID credentials.</li> <li>In the User Profile screen, enter your Standard ID password under To edit your user profile, please enter your password.</li> <li>Click Unlock.</li> <li>Click Generate an Identity Token.</li> <li>In the Generate an Identity Token dialog box, provide a meaningful description in the Add Description field (e.g., a purpose and today\u2019s date).</li> <li>Click Next.</li> <li>Copy your generated Reference Token by clicking on the Copy 'Generated token' to clipboard button.</li> <li>Return to your <code>pip.ini</code> file and replace each instance of <code>$API_KEY</code> with your unique token (four places).</li> <li>Save and close your <code>pip.ini</code> file.</li> <li>Run the following commands to create your virtual environment and test setup:</li> </ol> <pre><code># Bash commands to install Python packages\n# Assumes you have created the following location:\n# /c/gcp_sandbox/modelproject01/\n\ncd /c/gcp_sandbox/modelproject01/\nmkdir -p venvs\ncd venvs\npython -m venv venv\ncd venv/Scripts\nsource ./activate\npython -m pip install --upgrade pip\npython -m pip install scipy\n</code></pre> <p>Tip: If the <code>source</code> and <code>python</code> commands run without error, your Artifactory setup was successful.</p> <p>Additional Identity Token Characteristics  - If you make a mistake, you cannot reuse the same token; however, you can generate a new token by repeating steps 5 through 8. - Tokens cannot be deleted, so it is important to name them in a meaningful way. - Tokens expire after one year.</p>"},{"location":"onboarding_new_gcp_users/#4-troubleshooting-tips-for-this-section","title":"4. Troubleshooting Tips for this Section","text":"<p>If you have run <code>source ./activate</code> and later closed your terminal session, your session has been deactivated. You will need to reactivate it before continuing.</p> <p>Other common <code>pip.ini</code> issues: - You accidentally copied incorrect information from Artifactory to <code>pip.ini</code>. - An incorrect <code>pip.ini</code> file is being used or the one you built is not in the correct path. Run the following to verify:   <code>bash   echo $(realpath \"pip.ini\")</code>   Then review the previous instructions. - Your Standard ID was incorrectly entered into <code>pip.ini</code> (it must be in lowercase). - You forgot to replace all four instances of <code>$API_KEY</code> with your generated token.</p> <p></p>"},{"location":"onboarding_new_gcp_users/#writing-gcp-workflows","title":"Writing GCP Workflows","text":"<p>It is vital to understand that GCP is built for workflows designed to process large quantities of data. Be patient with the exercises that present the anatomy of a GCP workflow \u2014 they are intentionally designed to ensure that no node in a workflow is busy collecting data unnecessarily.</p> <p>To illustrate this, consider that most real-world projects will typically fall into one of these GCP kernel data-processing scenarios: - Node not executed previously, external data required, and node is available - Node not executed previously, external data required, but dataset not registered - Node has previously been executed</p> <p>For more about nodes, see the Nodes section in the API.</p> <p>If you want to run GCP and create a basic workflow, you do not need to deep dive into nodes or syntax yet (this can come later). Follow the instructions below to get started. If you encounter an impossible impasse, you can email GCP support.</p>"},{"location":"onboarding_new_gcp_users/#how-to-create-your-first-gcp-workflow","title":"How to Create Your First GCP Workflow","text":"<p>This section walks you through: - Downloading a repository - Configuring your Python virtual environment with the correct tools and libraries - Executing a simple workflow to add two variables (a+b) - Exiting the virtual environment gracefully so that you do not accidentally leave it open when switching to a different project with unique requirements</p> <p>To run your first workflow successfully, complete the following high-level steps:</p> <ol> <li>Prepare GCP and Install Test Environment</li> <li>Install GCP library by installing the GCP pip package</li> <li>Write workflow</li> <li>Test workflow</li> </ol>"},{"location":"onboarding_new_gcp_users/#1-prepare-gcp-and-install-test-environment","title":"1. Prepare GCP and Install Test Environment","text":"<p>You should have already installed the recommended Python version that includes Spark integration. See Installing Python and Git if needed.</p> <p>Since you will be running GCP in a virtual environment (venv), you are not actually installing GCP. Instead, you will copy a folder from the GRA GCP repository that contains workflow examples. The final steps of this section will focus on setting up your <code>venv</code>.</p> <p>Important: If you have not set up your SSH key to secure data traveling to and from Bitbucket repositories, complete Set up Bitbucket SSO first.</p> <p>To prepare your GCP repository:</p> <ol> <li>Open <code>bash</code> or a similar Unix shell and create a folder for GCP.</li> </ol> <p>Example:</p> <pre><code>~/c/gcp_sandbox/modelproject01/\n</code></pre> <p>Important: For faster performance, run your projects from the <code>C:</code> drive rather than the <code>H:</code> drive, especially when working in a virtual environment.</p> <ol> <li>Select your preferred repository with sample workflows in Bitbucket\u2019s GCP project directory here.</li> </ol> <p>Troubleshoot: If you cannot see the GCP repos, or if you receive an HTTPS port 443 error, you may lack Bitbucket access. See Getting the Right Permissions.</p> <ol> <li>With the repository selected (gcp_py_samples), click the Clone button on the left panel and copy the clone command.</li> </ol> <p>Example:</p> <pre><code>ssh://git@scm.horizon.bankofoz.com/gcp/gcp_py_samples.git\n</code></pre> <p>Screenshot: Clone gcp_py_samples for A+B Workflow </p> <ol> <li>Return to your shell (<code>bash</code>) and run the <code>git clone</code> command with the copied SSH string. Then enter that folder.</li> </ol> <p>Example:</p> <pre><code>cd /c/gcp_sandbox/modelproject01/\ngit clone ssh://git@scm.horizon.bankofoz.com/gcp/gcp_py_samples.git\n</code></pre> <ol> <li>Display the directory\u2019s contents and confirm cloning. Then <code>cd</code> into the local copy of the repo.</li> </ol> <p>Example:</p> <pre><code># Enter the directory and list contents\ncd gcp_py_samples\nls\n</code></pre> <p>If needed, you can locate a cloned repo with:</p> <pre><code>find /c/gcp_sandbox/ -type d -name gcp_py_samples -print\n</code></pre> <p>Tip: It is a good idea to complete this section before moving on. For each session, activate your virtual environment before using it, or create a launch batch file to automate this. If it is already activated and you activate it again, it will not drain additional resources.</p> <ol> <li>Now that you are in the cloned repository folder, instantiate your virtual environment (venv). This should take approximately 15 seconds.</li> </ol> <p>Example:</p> <pre><code># Create your venv\npython -m venv venv\n\n# *Reminder:* \n# The first \"venv\" is the Python module that creates virtual environments. \n# The second \"venv\" is the directory it creates to house your isolated environment.\n</code></pre> <p>Tip: Naming your virtual environment <code>venv</code> simplifies detection by tools like PyCharm. However, you may name it something else if needed (e.g., <code>gcp_py_samples_venv</code>).</p> <ol> <li>Activate the newly created virtual environment.</li> </ol> <p>Example:</p> <pre><code># For Linux or macOS syntax:\nsource venv/Scripts/activate\n\n# For Windows CMD:\nvenv\\Scripts\\activate\n</code></pre> <p>Benefit: After activation, any <code>pip install</code> commands will install packages inside this virtual environment. This allows easy environment control without affecting system-wide Python installations.</p>"},{"location":"onboarding_new_gcp_users/#2-install-gcp-library-by-installing-the-gcp-pip-package","title":"2. Install GCP Library by Installing the GCP pip Package","text":"<ol> <li>Install the preliminary GCP package and dependencies.</li> </ol> <p>Example:</p> <pre><code># Install GCP dependencies\npip install --no-deps -e .\n</code></pre> <p>If the command does not work, explicitly call Artifactory:</p> <p>Example:</p> <pre><code>pip install --no-deps -e . --extra-index-url https://&lt;StandardID&gt;:*****@artifacts.horizon.bankofoz.com/artifactory/api/pypi/virtual-pypi/simple\n</code></pre> <ol> <li>Install the specific libraries for this repository.</li> </ol> <p>In this repository, you are installing: - <code>gcp</code>, <code>matplotlib</code>, and <code>radon</code> from the <code>requirements.txt</code> file - <code>pytest</code> from the <code>build_requirements.txt</code> file</p> <p>Example:</p> <pre><code># Install main libraries\npip install -r requirements.txt\n\n# Install testing framework\npip install -r build_requirements.txt\n</code></pre> <p>Reference: For background on how <code>venv</code> environments are created and updated, see the official Python venv tutorial.</p> <ol> <li>If the above installations were successful, you are ready to proceed to the next section: Write Workflow.</li> </ol> <p>However, it is important to deactivate your virtual environment before switching to another project that uses a different repository.</p> <p>You can deactivate your environment by running:</p> <p>Example:</p> <pre><code># Exit the current venv\ndeactivate\n</code></pre> <p>Troubleshooting: If you were unsuccessful in this section, confirm that your <code>pip.ini</code> (or <code>pip.cfg</code>) file is correct, and that your path points properly to <code>/c/gcp_sandbox/modelproject01/</code>.</p>"},{"location":"onboarding_new_gcp_users/#know-when-to-deactivate-your-virtual-python-environment","title":"Know When to Deactivate Your Virtual Python Environment","text":"<ul> <li>When you need to switch to another virtual environment (for a different project)</li> <li>When you complete a project</li> <li>Before running system-wide Python commands (e.g., installing a global package)</li> <li>When opening a new shell session (because venvs do not persist across terminal sessions)</li> </ul>"},{"location":"onboarding_new_gcp_users/#3-write-workflow","title":"3. Write Workflow","text":"<p>Now it is time to write the first GCP workflow (<code>hello_world</code>), comprising two nodes:</p> <ul> <li>name: This element is defined as an axis of type <code>Str</code>. Axis values (coordinates) can be specified by a user during execution (at runtime). They can also be set by other nodes (detailed in a forthcoming walkthrough TK).</li> <li>message: This element is defined as a function (Fn). GCP function nodes accept any Python function (e.g., <code>say_hello</code>) and a mapping of outputs of another node to this function\u2019s inputs. In this graph, the <code>say_hello</code> function\u2019s input parameter <code>user_name</code> is being set to the value of the <code>name</code> node.</li> </ul> <p>To prepare your first GCP Python script:</p> <ol> <li> <p>Launch VS Code or any text editor that can be used for coding or scripting.</p> </li> <li> <p>From the GCP module, import the required functions or classes:</p> </li> </ol> <pre><code>from gcp_kernel.api.task_definition import gcp_task\nfrom gcp_kernel.api.workflow_definition import Workflow, Axis, Fn\n</code></pre> <ol> <li>Using f-string syntax, which concisely and efficiently manipulates strings in Python, create the greeting. It is important to use <code>@gcp_task</code>, a decorator, to define the greeting function:</li> </ol> <pre><code>@gcp_task\ndef say_hello(user_name: str) -&gt; str:\n    return f'Hello {user_name}'\n</code></pre> <p>Important: All functions called directly from a GCP workflow must use either the <code>@gcp_task</code> or <code>@gcp_spark</code> decorators. These decorators modify or extend the behavior of functions without changing their actual code.</p> <ol> <li>Construct your GCP workflow. A GCP workflow is a set of GCP nodes chained together:</li> </ol> <pre><code>greeter = Workflow(\n    workflow_name=\"Greeter\",\n    description=\"Workflow that creates a greeting\",\n    graph={\n        'name': Axis(str),\n        'message': Fn(say_hello, {'user_name': 'name'})\n    }\n)\n</code></pre> <p>Python concept: A graph is a Python data structure that consists of nodes and edges. The edges connect nodes and can be either bidirectional or directed (where one node specifically depends on another).</p> <ol> <li>Save your file:</li> </ol> <pre><code>../gcp_py_samples/hello_world_01.py\n</code></pre> <ol> <li>Copy your new file to the <code>src</code> folder:</li> </ol> <pre><code>cp hello_world_01.py /c/gcp_sandbox/modelproject01/gcp_py_samples/src/gcp_py_samples/workflows\n</code></pre> <p>Note: It is best practice to copy the file instead of moving it. This way, your copy remains easily discoverable by any GCP program or script looking for your workflow.</p> <p>Now that your file <code>hello_world_01.py</code> is saved into both your <code>gcp_py_samples</code> and <code>src</code> folders, you are ready to test it for errors and run it in the next section.</p> <p>A brief explanation of some key elements in your workflow will also be reviewed during testing, using <code>pytest.ini</code>.</p>"},{"location":"onboarding_new_gcp_users/#4-test-workflow","title":"4. Test Workflow","text":"<p>You will run the <code>hello_world_01.py</code> workflow by sending it values and testing parameters using a file that you will edit called <code>test_hello_world_01.py</code>.</p> <p>Normally, before releasing a GCP workflow, model developers test it with <code>pytest.ini</code>. This not only runs your workflow, but also identifies dozens of potential problems with your code, data binding, or delays.</p> <p>Diagram: hello_world_01.py ladder diagram summarizes how the files work together when you execute <code>pytest.ini</code> at runtime.</p> <p>To proceed:</p> <ol> <li> <p>In the tests/integration folder, make a copy of <code>test_demo.py</code> and rename it to <code>test_hello_world_01.py</code>.</p> </li> <li> <p>Open <code>test_hello_world_01.py</code> in VS Code or a text editor.</p> </li> <li> <p>Specify the correct file and workflow name (<code>greeter</code>) by editing line 2:</p> </li> </ol> <pre><code># Change:\n# from gcp_py_samples.workflows.demo import demo_wf\n\n# Update to:\nfrom gcp_py_samples.workflows.hello_world_01 import greeter\n</code></pre> <ol> <li>Update the node name and coordinates by editing lines 6 and 7:</li> </ol> <pre><code># Change:\n# node = 'FN2'\n# coords = ('AX1', 100)\n\n# Update to:\nnode = 'message'\ncoords = {'name': 'John Doe'}\n</code></pre> <ol> <li>Update the <code>ExecRequests</code> constructor with the correct <code>workflow_fqn</code> and <code>version</code> by editing lines 10 and 11:</li> </ol> <pre><code># Change:\n# workflow_fqn=demo_wf.fqn,\n# version=demo_wf.version,\n\n# Update to:\nworkflow_fqn=greeter.fqn,\nversion=greeter.version,\n</code></pre> <ol> <li>Specify the function that your test will be evaluating by updating the <code>assert</code> statement:</li> </ol> <pre><code># Change:\n# assert results.get('FN2') == '200 Hello'\n\n# Update to:\nassert results.get('message') == '200 Hello'\n</code></pre> <p>Note: <code>assert</code> is how tests pass or fail in <code>pytest</code>: - If the assertion fails, <code>pytest</code> marks the test as failed and shows a diff between expected and actual values. - If the assertion passes, the test continues or ends successfully (see Step 9 for more).</p> <ol> <li>After completing the edits, save the document to the integration folder:</li> </ol> <pre><code>/c/gcp_sandbox/modelproject01/gcp_py_samples/tests/integration/test_hello_world_01.py\n</code></pre> <ol> <li>Run the file you just saved and review the response to familiarize yourself with the feedback provided by the tests.</li> </ol> <p>Example:</p> <pre><code>C:\\dev\\gcp_py_samples\\tests\\integration&gt; pytest test_hello_world_01.py\n</code></pre> <ol> <li>To complete this exercise, change your <code>assert</code> argument to match the correct expected value and rerun <code>pytest</code>.</li> </ol> <p>Example:</p> <pre><code># Change:\n# assert results.get('message') == '200 Hello'\n\n# Update to:\nassert results.get('message') == 'Hello John Doe'\n</code></pre> <p>In the next section, you will run a similar workflow called <code>hello_world_02.py</code>. These instructions will be more concise and will introduce new nodes to add two numbers.</p> <p></p>"},{"location":"onboarding_new_gcp_users/#understanding-the-gcp-workflow","title":"Understanding the GCP Workflow","text":"<p>In this section you will:</p> <ul> <li>Create a workflow that adds two numbers</li> <li>Review key workflow configuration parameters</li> <li>Test several cases with a single test script</li> <li>Concisely summarize the first two workflows</li> <li>Learn how to utilize GCP tasks to re-use and repurpose functions</li> <li>Summarize how workflows and tasks work together</li> </ul>"},{"location":"onboarding_new_gcp_users/#1-how-to-create-a-workflow-adding-two-numbers","title":"1. How to Create a Workflow Adding Two Numbers","text":"<p>In this section, your objective is to create a GCP workflow that takes two input values (<code>x</code> and <code>y</code>) and returns their sum.</p> <p>Diagram: </p> <p>The hello_world_02.py Workflow Summary ladder diagram above reviews how the files work together when you execute <code>pytest.ini</code> at runtime. Refer back to this diagram after creating a similar file, hello_world_03.py, which introduces the GCP <code>@gcp_task</code> decorator and the tasks folder.</p>"},{"location":"onboarding_new_gcp_users/#key-workflow-configuration-parameters","title":"Key Workflow Configuration Parameters","text":"<p>Below is a list of the main parameters used in this exercise, which you will frequently use when designing your own workflows:</p> <ul> <li><code>Axis</code>: Represents an input node.</li> <li><code>Fn</code>: Represents a computation node that runs a Python function.</li> <li><code>function_path=add_two_numbers</code>: Directly references the local Python function.</li> <li><code>inputs={'x': 'input1', 'y': 'input2'}</code>: Binds input values to function arguments.</li> <li><code>output</code>: Holds the result of <code>add_two_numbers(x, y)</code>.</li> </ul> <p>Steps:</p> <ol> <li>Open <code>hello_world_01.py</code>, remove all code, add a comment summarizing the new function, and save the file as <code>hello_world_02.py</code>.</li> </ol> <p>Example:</p> <pre><code># GCP Workflow with function to add_two_numbers\n</code></pre> <ol> <li>Import the core GCP classes and utilities needed to define the workflow.</li> </ol> <p>Example:</p> <pre><code>from gcp_kernel.api.task_definition import gcp_task\nfrom gcp_kernel.api.workflow_definition import Axis, Fn, Workflow, WorkflowConf, ExecutionPlatform\n</code></pre> <ol> <li>Define a local function that adds two numbers.</li> </ol> <p>Example:</p> <pre><code># This is the actual Python logic in your GCP workflow\ndef add_two_numbers(x, y):\n    return x + y\n</code></pre> <p>Hint: You can later move this functionality into a separate task file. We will cover this in the next section: Learn How to Utilize GCP Tasks.</p> <ol> <li>Construct the <code>hello_world_02</code> workflow using the <code>Workflow</code> class.</li> </ol> <p>Example:</p> <pre><code>hello_world = Workflow(\n    workflow_name=\"Hello World 02\",\n    description=\"A workflow that sums its inputs\",\n    configurations=WorkflowConf(platform=ExecutionPlatform.PURE_PYTHON, is_deployable=False),\n    graph={\n        'input1': Axis(data_type=int),\n        'input2': Axis(data_type=int),\n        'output': Fn(function_path=add_two_numbers, inputs={'x': 'input1', 'y': 'input2'})\n    }\n)\n</code></pre> <ol> <li>Save the file. Then copy it into the <code>/src/workflows/</code> directory.</li> </ol> <p>Example:</p> <pre><code>cp hello_world_02.py /c/gcp_sandbox/modelproject01/gcp_py_samples/src/gcp_py_samples/workflows/hello_world_02.py\n</code></pre> <p>Tip: If you do not save the file into the workflows directory, you will not be able to import it correctly in your tests.</p> <ol> <li>Prepare a new test file by copying and updating <code>test_hello_world_01.py</code>, and saving it as <code>test_hello_world_02.py</code>.</li> </ol> <p>Example:</p> <pre><code># test_hello_world_02 function to add_two_numbers, x=3, y=7\n# Save to:\n# /c/gcp_sandbox/modelproject01/gcp_py_samples/tests/integration/\n\nfrom gcp_kernel.api.execution import ExecRequests\nfrom gcp_py_samples.workflows.hello_world_02 import hello_world\n\ndef test_addition_workflow(python_executor):\n    node = 'output'\n    coords = {'input1': 3, 'input2': 7}\n\n    exec_request = ExecRequests(\n        workflow_fqn=hello_world.fqn,\n        version=hello_world.version,\n        nodes_and_coords={node: coords}\n    )\n\n    results, _ = python_executor.execute_requests(exec_request)\n    assert results.get('output') == 10\n</code></pre> <ol> <li>Run the file you just saved and review the response.</li> </ol> <p>Example:</p> <pre><code>/c/gcp_sandbox/modelproject01/gcp_py_samples/tests/integration&gt; pytest test_hello_world_02.py\n</code></pre>"},{"location":"onboarding_new_gcp_users/#2-experiment-with-assert-for-better-test-scripting","title":"2. Experiment with <code>assert</code> for Better Test Scripting","text":"<p>Testing, as you may have noticed from the output reports, is multi-layered and comprehensive. You can take advantage of this by modifying test scripts to create multiple test cases that run each time you call a test file. Simply use a parameterized approach.</p> <p>In a Nutshell: Copy everything after the import statements, paste it at the bottom of the file, give the function a unique name, and modify your values. The example below shows how you can easily run two tests from a single file.</p> <p>Example:</p> <pre><code># File: ../gcp_py_samples/tests/integration/test_hello_world_02_two_asserts.py\n\nfrom gcp_kernel.api.execution import ExecRequests\nfrom gcp_py_samples.workflows.hello_world_02 import hello_world\n\ndef test_addition_workflow(python_executor):\n    node = 'output'\n    coords = {'input1': 3, 'input2': 7}\n\n    exec_request = ExecRequests(\n        workflow_fqn=hello_world.fqn,\n        version=hello_world.version,\n        nodes_and_coords={node: coords}\n    )\n\n    results, _, _ = python_executor.execute_requests(exec_request)\n    assert results.get('output') == 10\n\ndef test_addition_workflow02(python_executor):\n    node = 'output'\n    coords = {'input1': 4, 'input2': 8}\n\n    exec_request = ExecRequests(\n        workflow_fqn=hello_world.fqn,\n        version=hello_world.version,\n        nodes_and_coords={node: coords}\n    )\n\n    results, _, _ = python_executor.execute_requests(exec_request)\n    assert results.get('output') == 13\n</code></pre>"},{"location":"onboarding_new_gcp_users/#3-concise-summary-of-first-two-workflows","title":"3. Concise Summary of First Two Workflows","text":"<p>This section provides a quick summary of the GCP elements involved in <code>say_hello()</code> and <code>add_two_numbers(x, y)</code>. Since each exercise included some setup tasks that you only need to do once, this serves as a starting point for quick reviews \u2014 focusing purely on GCP elements and key Python concepts such as decorators.</p>"},{"location":"onboarding_new_gcp_users/#in-say_hello-you-ran-a-basic-workflow-with-two-files","title":"In <code>say_hello()</code>, you ran a basic workflow with two files:","text":"<ul> <li><code>hello_world_01.py</code> defined a function (<code>say_hello(name)</code>)</li> <li><code>test_hello_world_01.py</code> tested this function</li> </ul> <p>The test simulated a greeting output and validated the result:</p> <ul> <li>\u2611 If the result matches expectations \u2192 the test passes</li> <li>\u2717 If it does not match \u2192 the test fails</li> </ul>"},{"location":"onboarding_new_gcp_users/#in-add_two_numbersx-y-you-built-a-gcp-workflow-that-adds-two-numbers","title":"In <code>add_two_numbers(x, y)</code>, you built a GCP workflow that adds two numbers:","text":"<ul> <li><code>hello_world_02.py</code> defined the workflow inline, using:</li> <li><code>Axis</code> for input nodes</li> <li><code>Fn</code> for the function node</li> <li> <p>The <code>add_two_numbers(x: int, y: int)</code> function, written directly in the file</p> </li> <li> <p><code>test_hello_world_02.py</code> submitted an execution request with inputs and asserted the result</p> </li> </ul> <p>In the test output (echo):</p> <ul> <li>\u2611 If the sum matches \u2192 the test passes</li> <li>\u2717 If the result is incorrect or the type hint is missing \u2192 the test fails</li> </ul>"},{"location":"onboarding_new_gcp_users/#4-learn-how-to-utilize-gcp-tasks","title":"4. Learn How to Utilize GCP Tasks","text":"<p>The objective of this section is to offload core business logic to a third file, stored in the <code>../gcp_py_samples/src/gcp_py_samples/tasks/</code> folder. You will then access the function from workflows using the <code>@gcp_task</code> decorator.</p> <ol> <li>Copy <code>hello_world_02.py</code> to a new file</li> </ol> <p>Create a copy of the script that added two numbers into the <code>workflows</code> folder.</p> <p>Example:</p> <pre><code>cp hello_world_02.py /c/gcp_sandbox/modelproject01/gcp_py_samples/src/gcp_py_samples/workflows/hello_world_03.py\n</code></pre> <ol> <li>Remove the function that will be placed into a new file</li> </ol> <p>Delete or comment out the function, as well as the import code that is no longer needed. In the next step, this logic will be moved to a separate Python file.</p> <p>Example:</p> <pre><code># GCP Workflow with function to add_two_numbers\n\n# Commented out import (1 line):\n# from gcp_kernel.api.task_definition import gcp_task\n\nfrom gcp_kernel.api.workflow_definition import Axis, Fn, Workflow, WorkflowConf, ExecutionPlatform\n\n# ...\n\n# Commented out core logic (3 lines):\n# @gcp_task\n# def add_two_numbers(x: int, y: int) -&gt; int:\n#     return x + y\n\nhello_world = Workflow(\n    # ...\n)\n</code></pre> <ol> <li>Create the task file</li> </ol> <p>Move the removed function into a new file called <code>add_two_numbers_03.py</code>, and save it in the <code>tasks</code> folder:</p> <pre><code>/c/gcp_sandbox/modelproject01/gcp_py_samples/src/gcp_py_samples/tasks/\n</code></pre> <p>Contents of the new task file:</p> <pre><code>from gcp_kernel.api.task_definition import gcp_task\n\n@gcp_task\ndef add_two_numbers(x: int, y: int) -&gt; int:\n    \"\"\"Task that returns a sum of its inputs\"\"\"\n    return x + y\n</code></pre> <ol> <li>Import the task into your workflow</li> </ol> <p>In the import section of <code>hello_world_03.py</code>, add your new task import.</p> <p>Example:</p> <pre><code># Necessary import when moving the function to the tasks folder\n\nfrom gcp_py_samples.tasks.add_two_numbers_03 import add_two_numbers\n</code></pre> <p>You can now access your stand-alone task from any workflow.</p> <ol> <li>Create a new test file (optional)</li> </ol> <p>Although you could reuse <code>test_hello_world_02.py</code>, it is good practice to create a discrete test file.</p> <p>Example:</p> <pre><code>cp test_hello_world_02.py /c/gcp_sandbox/modelproject01/gcp_py_samples/src/gcp_py_samples/tests/test_hello_world_03.py\n</code></pre> <ol> <li>Run the test</li> </ol> <p>Modify the new or existing test script as needed and run it.</p> <p>Example:</p> <pre><code>pytest test_hello_world_03.py\n</code></pre>"},{"location":"onboarding_new_gcp_users/#5-summary-workflows-and-tasks","title":"5. Summary: Workflows and Tasks","text":"<p>In the previous section, your objective was to separate a GCP workflow from the task of adding two input values (<code>x</code> and <code>y</code>). Three <code>.py</code> files are involved, summarized in the ladder diagram below:</p> <p>Diagram: </p> <p>The hello_world_03.py Workflow Summary diagram reviews how <code>pytest.ini</code> and the three files work together when you execute the test at runtime. The GCP decorator <code>@gcp_task</code> is the most important element in this structure. Remember also to save your task in the correct tasks folder.</p> <p>Now that you have completed an overview of workflows and tasks, explore and browse the sample files available in these directories to experiment further:</p> <ul> <li><code>C:/dev/gcp_py_samples/src/gcp_py_samples/workflows/</code></li> <li><code>C:/dev/gcp_py_samples/src/gcp_py_samples/tasks/</code></li> </ul> <p></p>"},{"location":"onboarding_new_gcp_users/#gcp-workflow-components","title":"GCP Workflow Components <p>This section details the main workflow components, configuration parameters, and principles. It defines how workflows are constructed, how nodes operate, and provides examples for each node type.</p>","text":""},{"location":"onboarding_new_gcp_users/#workflow","title":"Workflow","text":"<p>A <code>Workflow</code> orchestrates a series of interconnected nodes \u2014 inputs, computations, and functions \u2014 into a graph structure. It defines the sequence and relationships between tasks that run within GCP.</p> <p>Example:</p> <pre><code># Created in Python using:\nfrom gcp_kernel.api.workflow_definition import Workflow\n</code></pre> <p>Additional Information: A GCP <code>Workflow</code> is conceptually similar to a DAG (Directed Acyclic Graph) in frameworks such as Airflow, Prefect, or Luigi.</p>"},{"location":"onboarding_new_gcp_users/#node","title":"Node","text":"<p>A <code>node</code> represents an individual component inside a workflow. Each node can be a data input, a computation, a constant, a function call, or other operation.</p> <p>Current GCP node types:</p> <ul> <li>Axis</li> <li>Const</li> <li>Fn</li> <li>Switch</li> <li>Ref</li> <li>UserInput</li> <li>SQL</li> <li>ExtSQL</li> <li>AggZip</li> </ul> <p>Each type is described below.</p>"},{"location":"onboarding_new_gcp_users/#axis","title":"Axis","text":"<p>An <code>Axis</code> represents a tunable or input value passed at runtime. It accepts values either from the user or from other nodes.</p> <p>Example:</p> <pre><code>'name': Axis(str)\n</code></pre> <p>Note: <code>Axis</code> behaves like a function parameter or loop variable in Python.</p>"},{"location":"onboarding_new_gcp_users/#const","title":"Const","text":"<p>A <code>Const</code> node holds a static value defined directly in the graph. It is used for constants that do not require dynamic input.</p> <p>Example:</p> <pre><code>'const': Const(List[int], [1, 2, 3])\n</code></pre> <p>Note: Python does not enforce constant variables; values are reassigned freely.</p>"},{"location":"onboarding_new_gcp_users/#fn","title":"Fn","text":"<p>An <code>Fn</code> node executes a Python function decorated with <code>@gcp_task</code> or <code>@gcp_spark_task</code>. It maps inputs from other nodes to the function\u2019s parameters.</p> <p>Example:</p> <pre><code>'message': Fn(say_hello, {'user_name': 'name'})\n</code></pre> <p>Note: <code>Fn</code> is equivalent to decorated function calls in Python frameworks.</p>"},{"location":"onboarding_new_gcp_users/#switch","title":"Switch","text":"<p>A <code>Switch</code> node implements conditional branching, similar to Python\u2019s <code>if-elif-else</code> or <code>match-case</code> logic.</p> <p>Example:</p> <pre><code>\"result\": Switch(\"x\", {1: \"frac#denominator\", 2: \"frac#numerator\"}, \"frac#numerator\")\n</code></pre>"},{"location":"onboarding_new_gcp_users/#ref","title":"Ref","text":"<p>A <code>Ref</code> node references another node\u2019s output or a workflow\u2019s result. It allows reuse of values computed elsewhere.</p> <p>Example:</p> <pre><code>\"prediction\": Ref('gcp_py_samples.workflows.demo2b.calbr_wf/model_output',\n                  {'portfolio_name': 'portfolio_name', 'as_of': 'as_of'})\n</code></pre> <p>Note: In Python, references are usually implicit through variable assignment.</p>"},{"location":"onboarding_new_gcp_users/#userinput","title":"UserInput","text":"<p>A <code>UserInput</code> node explicitly requires manual input at runtime before dependent nodes can execute.</p> <p>Example:</p> <pre><code>python_executor.add_user_input_value(...)\n</code></pre> <p>Note: This is conceptually similar to using <code>input()</code> in Python.</p>"},{"location":"onboarding_new_gcp_users/#sql","title":"SQL","text":"<p>An <code>SQL</code> node executes a database query inside the GCP environment against a predefined source.</p> <p>Example:</p> <pre><code>def sql_user(table: PORTFOLIO_SDF) -&gt; int:\n    return table.shape[0]\n</code></pre> <p>Note: In Python, similar operations are performed with libraries like <code>sqlite3</code>, <code>pandas.read_sql</code>, or SQLAlchemy.</p>"},{"location":"onboarding_new_gcp_users/#extsql","title":"ExtSQL","text":"<p>An <code>ExtSQL</code> node executes queries against external (non-Spark, non-Hive) databases.</p> <p>Example:</p> <pre><code>'sql_result': ExtSQL(\n    sql=sql_string,\n    sources=[\"CLIMATE\", \"WHOLESALE\"],\n    output_type=PORTFOLIO_SDF,\n    query_parameters={\"run_id\": \"run_id\"}\n)\n</code></pre> <p>Note: Python equivalents include external connectors such as <code>psycopg2</code> or <code>pyodbc</code>.</p>"},{"location":"onboarding_new_gcp_users/#aggzip","title":"AggZip","text":"<p><code>Agg</code> nodes perform aggregation over multiple node outputs. <code>Zip</code> nodes combine coordinate values element-wise across nodes.</p> <p>Aggregation Example:</p> <pre><code>'ag': Agg('fn', {'a': 'const'})\n</code></pre> <p>Zip Example:</p> <pre><code>'zip_nd': Zip(invoked_node='fn_nd2', sourced_axis_entries={'axis_int': 'cnst_nd2', 'axis_str': 'cnst_nd'})\n</code></pre> <p>Note: Python uses functions like <code>zip()</code>, <code>sum()</code>, and <code>reduce()</code> for similar purposes.</p>"},{"location":"onboarding_new_gcp_users/#summary-gcp-nodes-compared-to-python","title":"Summary: GCP Nodes Compared to Python","text":"GCP Concept Used in GCP Native in Python Equivalent Concept in Python Workflow \u2611\ufe0e \u2717 DAGs, pipelines (Airflow, Prefect) Node \u2611\ufe0e \u2611\ufe0e Functions, variables Axis \u2611\ufe0e \u2611\ufe0e Function parameters, input variables Const \u2611\ufe0e \u2717 Regular variables Fn \u2611\ufe0e \u2611\ufe0e (decorators) Decorated functions Switch \u2611\ufe0e \u2611\ufe0e if-elif-else, match-case Ref \u2611\ufe0e \u2611\ufe0e (implicit) Passing variables between functions UserInput \u2611\ufe0e \u2611\ufe0e (different) input() calls SQL \u2611\ufe0e \u2611\ufe0e (via libraries) Raw SQL queries (sqlite3, pandas) ExtSQL \u2611\ufe0e \u2611\ufe0e (via connectors) External DB connectors (psycopg2, pyodbc) AggZip \u2611\ufe0e \u2611\ufe0e (Zip only) zip(), sum(), reduce(), Pandas groupby().agg()"},{"location":"onboarding_new_gcp_users/#running-in-a-lane","title":"Running in a Lane <ul> <li>Overview of GCP SDLC process</li> <li>How to push workflows to a lane and execute from the UI</li> <li>Recommended starting point: Set Up SSH Keys for New GCP Servers</li> </ul>","text":""},{"location":"onboarding_new_gcp_users/#system-tasks","title":"System Tasks","text":"<ul> <li>Documentation for system tasks</li> <li>Writing unit tests and using the GCP internal test harness</li> </ul> <p>Note: <code>\"==\"</code> in comments is intentional \u2014 it is used for extended command testing across interpreters.</p> <p></p>"},{"location":"onboarding_new_gcp_users/#other-gcp-concepts","title":"Other GCP Concepts","text":""},{"location":"onboarding_new_gcp_users/#gcp-retrieves-and-persists-data-automatically","title":"GCP Retrieves and Persists Data Automatically","text":"<ul> <li>All data is indexed by axes.</li> <li>Axes are attached to outputs and stored persistently.</li> <li>(Additional details forthcoming.)</li> </ul>"},{"location":"onboarding_new_gcp_users/#best-practices","title":"Best Practices","text":"<ul> <li>Configure result persistence early in development.</li> <li>Design modular workflows to support \"what-if\" analysis scenarios.</li> <li>(Additional best practices forthcoming.)</li> </ul>"},{"location":"onboarding_new_gcp_users/#document-approval-and-acceptance-history","title":"Document Approval and Acceptance History    Doc Version Go Live Date Approved by Notes and Comments     v.0.0.1 2025-03-01 Adil Imam Initial draft, will later split into subdocuments   v.1.0.0 2025-04-14 Adil Imam Major revisions by Adriano Sverko, milestone release   v.1.1.0 tk tk tk   v.2.0.0 tk tk tk","text":""}]}